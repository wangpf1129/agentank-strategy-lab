# Data

Historical raw data was deleted during the lean refactor.

Do not rebuild a raw data archive in the repo. Fetch a specific replay or run output to `/tmp/agentank-runs/` only when the current task names a match id, run id, or leaderboard timestamp.

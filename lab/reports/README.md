# Reports

Historical long reports were deleted during the lean refactor.

For active work, update `state/cycle.md` instead of creating a long report. Create a short report here only when it directly supports a current publish, rollback, or real challenge decision.
